export default interface ImasCharacters {
  id: number;
  name: string;
  // eslint-disable-next-line camelcase
  name_kana: string;
  age: number;
  bust: number;
  hip: number;
  waist: number;
  color: string;
  // eslint-disable-next-line camelcase
  voice_actor: string;
  title: string;
}
